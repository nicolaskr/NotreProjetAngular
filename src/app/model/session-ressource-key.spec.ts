import { SessionRessourceKey } from './session-ressource-key';

describe('SessionRessourceKey', () => {
  it('should create an instance', () => {
    expect(new SessionRessourceKey()).toBeTruthy();
  });
});
